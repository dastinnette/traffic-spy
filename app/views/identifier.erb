<div class="jumbotron">
  <h1><%= @identifier.identifier %></h1>
  <br>
    <p>
      <% sort_url_requests.each do |url, count| %>
      <p><%= "#{url} : Was requested #{count} time(s)" %><br></p>
      <br>
      <% end %>
    </p>
    <p>
      <%= web_browser_report %>
    </p>
    <p>
      <%= operating_system_report %>
    </p>
    <p>
      <%= resolution_dimensions %>
    </p>
</div>
